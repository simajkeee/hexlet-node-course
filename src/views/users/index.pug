extends ../layouts/page.pug

block content
    div
        if users.length === 0
            p No users found
        else
            each user in users
                div
                    h2
                        a(href=`${route('user', { id: user.id })}`)= user.title
                    p= user.email
